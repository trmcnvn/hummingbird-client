<div class="jumbotron jumbotron-kitsu">
  <div class="container">
    <div class="row">
      <h2>{{t "feedback.titles.feedback"}}</h2>
      <nav class="nav">
        {{#link-to-kitsu "feedback.bugs" class="nav-link"}}
          {{t "feedback.titles.bugs"}}
        {{/link-to-kitsu}}
        {{#link-to-kitsu "feedback.feature-requests" class="nav-link"}}
          {{t "feedback.titles.feature-requests"}}
        {{/link-to-kitsu}}
        {{#link-to-kitsu "feedback.database-requests" class="nav-link"}}
          {{t "feedback.titles.database-requests"}}
        {{/link-to-kitsu}}
        {{#link-to-kitsu "feedback.mobile-bugs" class="nav-link"}}
          {{t "feedback.titles.mobile-bugs"}}
        {{/link-to-kitsu}}
        {{#link-to-kitsu "feedback.mobile-features" class="nav-link"}}
          {{t "feedback.titles.mobile-features"}}
        {{/link-to-kitsu}}
      </nav>
    </div>
  </div>
</div>

{{! Canny embed }}
<div class="container">
  <div class="row">
    <div class="d-flex w-100" data-canny></div>
    {{canny-script token=model.token boardToken=model.boardToken path=model.path}}
  </div>
</div>