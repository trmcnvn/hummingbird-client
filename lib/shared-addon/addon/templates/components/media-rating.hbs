{{#if hasRating}}
  {{#if isReadOnly}}
    {{component (concat "media-rating/" ratingComponent)
      rating=rating
      isReadOnly=isReadOnly
      onRating=(action onRating)}}
  {{else}}
    {{! @TODO!!!! }}
  {{/if}}
{{/if}}
